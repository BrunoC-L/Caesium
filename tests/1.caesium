type A:
    using C = B

type B:
    using C = B

type T:
    A<B> a
    B::C b
    A<B>::C c
    A<B::C> d

type F:

type E:
    using G = A
    using T = F

type R extends F<V,F<U, V>>, F<E<B>,A<B>>:
    using C = A<B>
    using K = F<B, C>
    E<K>::T<B, C> member1
    E<K>::G method1(K k, C u):
        Set<int> someContainer
        for i in someContainer:

        int arr
        for i in arr:

        Map<int, std::string> m
        for k, v in m.entries(normal()[][x,z,y]):
            ++k
            k++
            --k
            k--
            k+=1
            k-=1
            k * 1
            k + 1
            k - 1
            1 % k
            (k)[k][k,k.k()[k]]+=k
