#pragma once
#include <memory>
#include <utility>
#include <variant>
#include <vector>
#include <string>
#include <sstream>
#include <fstream>


struct A {
	A(C c) {
		this.c=c();
		for (auto& forstatementvar : a) {
			auto& [i, e] = forstatementvar;
			if (b)
				return c;
			if (c)
				break;
		}
	}
};

