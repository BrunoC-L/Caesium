type A:
	A(C c):
        return a

A a():
    return A{} if false
    while true:
        Set<A> b = {}
        Bool c = false
        Bool d = True
        for a in b if c while d:
            break if d
        for a in b while d:
            return a if c else b
        a = f if e else a
        return A() if b == b

Int main():
    return 0
