Bool test_transpile_all_folders(auto ref folders):
    for dir in folders:
        match dir:
            directory d:
                return test_transpile_folder(d)
            file f:
    return True


Bool test_transpile_folder(directory ref dir):
    return False
