interface Named:
    String name

type Cat:
    String name

type Dog:
    String name

Int main(Vector<String> ref s):
    Vector<Named> vec = {}
    vec.push(Dog{ "doggo" })
    vec.push(Cat{ "gato"  })
    for e in vec:
        println(e)
    return 0
