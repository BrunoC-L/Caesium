interface Named:
    String name

type Cat:
    String name

type Dog:
    String name

type CatsAndDogs:
    Vector<Cat> cats
    Vector<Dog> dogs

auto add(CatsAndDogs val cats_and_dogs, Cat val cat):
    cats_and_dogs.cats.push(cat)
    return cats_and_dogs

Int main(Vector<String> ref s):
    auto c = CatsAndDogs{}
    auto d = (move c).add(Cat{})
    return 0
