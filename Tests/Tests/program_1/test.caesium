type Dog:
type Cat:

using Animal = Dog | Cat

template <T>
Int size(Vector<T> ref vec):
    return 0

Int main(Vector<String> ref s):
    Vector<Animal> animals = {}
    return size<Animal>(animals)
