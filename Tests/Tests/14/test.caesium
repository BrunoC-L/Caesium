type Dog:
type Cat:

using Animal = Dog | Cat

template f<T>:
    Int f(Vector<T> ref vec):
        return 0

Int main(Vector<String> ref s):
    Vector<Animal> animals = {}
    return f<Animal>(animals)
